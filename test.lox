ফাংশন testArrayAndObject() {
    // Declare and manipulate an array
    ধরি arr = [10, 20, 30];
    দেখাও "The first element of arr: " + arr[0]; 
    arr[2] = 300;  // Modify the third element
    দেখাও "Modified third element of arr: " + arr[2];

    // Declare and manipulate an object
    ধরি obj = {
        name: "Bangla Interpreter",
        count: 1
    };
    দেখাও "Object name property: " + obj.name;
    obj.count = obj.count + 1; 
    দেখাও "Updated count property: " + obj.count;

    // Optionally return the object if you want to use it outside
    ফেরত obj; 
}

// Call the function to see the output
ধরি result = testArrayAndObject();
দেখাও "Returned object name: " + result.name;
দেখাও "Returned object count: " + result.count;
